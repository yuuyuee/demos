#include <iostream>
#include <ios>
#include <sstream>
#include <fstream>
#include <string>

int main() {
  std::fstream fs;
  std::stringstream ss;
  std::ostream os;
  std::iostream ios;


  std::streambuf sbuf;

  std::stringbuf strbuf;
  std::string s;
  return 0;
}
